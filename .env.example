# "paper" = simulate trades using real live prices (no real money moves)
# "live"  = place real orders (requires keys below, use with extreme caution)
MODE=paper

# Binance API credentials (only needed for live mode)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Hyperliquid wallet (only needed for live mode)
HL_PRIVATE_KEY=
HL_WALLET_ADDRESS=